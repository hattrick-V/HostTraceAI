# HostTraceAI 安全说明

HostTraceAI 是高权限主机溯源和事件响应平台，只能用于明确授权的主机。

默认安全边界：

- 采集优先只读；
- SSH 凭据不得写入 Skill、对话或日志；
- 处置动作需要策略检查和人工审批；
- 删除文件、密码尝试、横向连接、网段扫描和未知脚本默认禁止；
- 样本和证据包按主机、任务隔离保存，并记录 SHA-256；
- C2 默认关闭，不提供攻击载荷或漏洞利用流程。

发现平台自身缺陷时，请不要提交真实凭据、样本或生产主机信息。
